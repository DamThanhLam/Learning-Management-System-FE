import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains: ['localhost', 'picsum.photos','lam-dev-iuh.s3.ap-southeast-1.amazonaws.com'],
  },

};

export default nextConfig;